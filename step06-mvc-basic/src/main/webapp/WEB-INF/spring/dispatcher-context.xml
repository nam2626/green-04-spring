<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           https://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- 
		context:component-scan
		지정한 패키지(com.spring.controller) 하위에서 
		@Controller, @Service, @Repository, @Component 등의 어노테이션이 붙은 클래스를 찾아 
		Spring Bean으로 자동 등록합니다.
	-->
	<context:component-scan base-package="com.spring.controller"/>
	
	<!-- 
		mvc:annotation-driven
		Spring MVC의 다양한 어노테이션(@RequestMapping, @GetMapping 등)을 처리하기 위한 
		필수 빈(RequestMappingHandlerMapping, RequestMappingHandlerAdapter 등)들을 
		자동으로 등록해줍니다.
	-->
	<mvc:annotation-driven/>
	
	<!-- 
		mvc:resources
		이미지, CSS, 자바스크립트와 같은 정적 자원(Static Resources)에 대한 요청을 처리합니다.
		mapping="/resources/**": /resources/ 로 시작하는 URL 요청을 
		location="/resources": 실제 웹 애플리케이션의 /resources 폴더에서 찾겠다는 의미입니다.
	-->
	<mvc:resources location="/resources" mapping="/resources/**"/>

	<!-- 
		InternalResourceViewResolver
		컨트롤러에서 리턴한 문자열(뷰 이름)을 바탕으로 실제 JSP 파일의 경로를 완성합니다.
		- prefix: 뷰 이름 앞에 붙을 경로
		- suffix: 뷰 이름 뒤에 붙을 확장자
		예: 컨트롤러에서 "home" 리턴 -> "/WEB-INF/views/" + "home" + ".jsp" = "/WEB-INF/views/home.jsp"
	-->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
</beans>










