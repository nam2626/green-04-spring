<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- 
        DispatcherServlet 등록
        Spring MVC의 핵심 서블릿입니다. 모든 요청을 제일 먼저 받아서 
        각 컨트롤러에게 요청을 배분(Dispatch)하는 역할을 합니다.
    -->
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <!-- 
                Spring MVC 설정 파일의 위치를 지정합니다. 
                여기서 빈(Bean) 등록, ViewResolver 설정 등을 수행합니다.
            -->
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/dispatcher-context.xml</param-value>
        </init-param>
        <!-- 서블릿 컨테이너(Tomcat)가 시작될 때 즉시 서블릿을 생성하도록 설정합니다. -->
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <!-- dispatcher 서블릿이 처리할 URL 패턴을 지정합니다. / 는 모든 요청을 처리함을 의미합니다. -->
    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <!-- 
        한글 인코딩 필터 (CharacterEncodingFilter)
        POST 방식 등으로 전달되는 데이터의 한글 깨짐 방지를 위해 인코딩을 UTF-8로 설정합니다.
    -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>
