<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           https://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        [복습 포인트 3] 컴포넌트 스캔
        - com.spring.review 하위의 @Controller, @Component 등을 자동 등록
    -->
    <context:component-scan base-package="com.spring.review"/>

    <!--
        [복습 포인트 4] 어노테이션 기반 MVC 활성화
        - RequestMappingHandlerMapping, RequestMappingHandlerAdapter 등을 자동 등록
        - @RequestMapping, @PathVariable, @RequestParam 등이 동작하려면 필수
    -->
    <mvc:annotation-driven/>

    <!-- 정적 자원(CSS, JS, 이미지) 처리 -->
    <mvc:resources location="/resources/" mapping="/resources/**"/>

    <!--
        [복습 포인트 5] ViewResolver
        - 컨트롤러가 반환한 뷰 이름 앞뒤에 prefix/suffix를 붙여 JSP 경로를 완성
        - 예: "products/list" → /WEB-INF/views/products/list.jsp
    -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

</beans>
